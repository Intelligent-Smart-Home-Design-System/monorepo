# Moved to monorepo root: ../../docker-compose.pipeline.yaml
#
# From monorepo root:
#   make pipeline-up
#   make pipeline-up-shifted
#   make pipeline-trigger
#
# Or explicitly:
#   docker compose -f docker-compose.pipeline.yaml up -d
# TODO delete this file

include:
  - path: ../../docker-compose.pipeline.yaml

