{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SmartHub",
  "description": "A smart home hub, gateway, or bridge device",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Product name as it appears in the listing, e.g. 'Aqara Hub M2'"
    },
    "brand": {
      "type": "string",
      "description": "Normalized lowercase brand name, e.g. 'aqara', 'xiaomi', 'sonoff'"
    },
    "model": {
      "type": "string",
      "description": "Model identifier as stated by the manufacturer, e.g. 'HM2-G01'."
    },
    "ecosystem": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "yandex",
          "apple",
          "google",
          "aqara",
          "tuya",
          "xiaomi",
          "digma",
          "sber",
          "vk",
          "ekf"
        ]
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "Smart home ecosystems this hub connects to or bridges into."
    },
    "protocol": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "zigbee",
          "z-wave",
          "wifi",
          "bluetooth",
          "ble",
          "matter-over-wifi",
          "matter-over-thread"
        ]
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "Wireless protocols this hub supports for connecting child devices, e.g. ['zigbee', 'wifi'] for a hub that bridges Zigbee devices to Wi-Fi."
    },
    "power_source": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "mains",
          "battery",
          "rechargeable_battery",
          "usb",
          "poe",
          "ac_dc_adapter"
        ]
      },
      "uniqueItems": true,
      "description": "Power sources supported. Most hubs are mains or usb powered. ac_dc_adapter = powered via an included AC/DC adapter plugged into a wall socket."
    },
    "max_connected_devices": {
      "type": "integer",
      "minimum": 1,
      "maximum": 500,
      "description": "Maximum number of child devices this hub can connect simultaneously, as stated by manufacturer."
    },
    "has_local_processing": {
      "type": "boolean",
      "description": "Whether automations and device control work locally without internet connection."
    },
    "has_ir_blaster": {
      "type": "boolean",
      "description": "Whether the hub includes an infrared blaster for controlling IR-based devices like TVs and air conditioners."
    },
    "wired_connection": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "ethernet",
          "usb_host"
        ]
      },
      "uniqueItems": true,
      "description": "Wired connectivity options. ethernet = RJ45 port for wired network connection. usb_host = USB port for connecting USB dongles or devices."
    },
    "matter_bridge": {
      "type": "boolean",
      "description": "Whether this hub acts as a Matter bridge, exposing connected non-Matter devices to Matter-compatible ecosystems like Apple Home or Google Home."
    },
    "zigbee_version": {
      "type": "string",
      "enum": [
        "zigbee_1.2",
        "zigbee_3.0"
      ],
      "description": "Zigbee protocol version supported, if applicable. zigbee_3.0 is backward compatible with 1.2 devices."
    },
    "type": {
      "const": "smart_hub"
    }
  },
  "required": [
    "name",
    "brand",
    "model",
    "ecosystem",
    "protocol",
    "type"
  ],
  "additionalProperties": false
}