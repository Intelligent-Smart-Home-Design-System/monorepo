# Extractor service

## Usage

Для запуска сервиса и обработки всех снепшотов серебряного слоя:
```sh
uv sync
uv run extractor run [args]
```

Для запуска тестов с метриками:
```sh
uv run extractor run-evaluation [args]
```

Для запуска примера
```sh
uv run extractor run-sample
```
